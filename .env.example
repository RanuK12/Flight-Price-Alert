# ===========================================
# Flight Deal Bot - Configuración
# ===========================================

# Puerto del servidor (default: 4000)
PORT=4000

# Entorno (development, production)
NODE_ENV=development

# Telegram - OBLIGATORIO para notificaciones
# 1. Habla con @BotFather y crea un bot → te da el TOKEN
# 2. Habla con @userinfobot → te da tu CHAT_ID
# Para múltiples usuarios, separar con comas: id1,id2
TELEGRAM_BOT_TOKEN=tu_bot_token
TELEGRAM_CHAT_ID=8107555656,832972739

# Frecuencia de búsqueda (cron) - cada 30 minutos
MONITOR_SCHEDULE=*/30 * * * *

# Auto-iniciar monitoreo (true por defecto en Railway)
AUTO_MONITOR=true

# ===========================================
# Puppeteer / Chrome - IMPORTANTE PARA SERVIDORES
# ===========================================
# Ruta al ejecutable de Chrome/Chromium en tu servidor
# Dejar vacío para usar el Chromium bundled de Puppeteer
#
# Ejemplos comunes:
# Linux (apt): /usr/bin/chromium-browser o /usr/bin/chromium
# Linux (snap): /snap/bin/chromium
# Docker: /usr/bin/google-chrome-stable
# Railway: chromium (se detecta automáticamente)
#
PUPPETEER_EXECUTABLE_PATH=
